# =============================
# 🎙️ Mongolian Whisper API (Local + Render)
# FastAPI + HuggingFace Whisper + Faster-Whisper
# =============================

python-dotenv==1.0.1

# --- API & Server ---
fastapi==0.115.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20
aiofiles==24.1.0
requests==2.32.3
pydantic==2.9.2
psutil==5.9.8

# --- ML & NLP ---
numpy==1.26.4
soundfile==0.12.1
pydub==0.25.1
sentencepiece==0.1.99

# --- TTS ---
edge-tts==6.1.12

# --- HuggingFace Whisper (HF fallback mode) ---
# ✅ Aligned with Colab Golden stack (tokenizer.json parsing compatibility)
transformers==4.49.0
accelerate==0.25.0
huggingface_hub==0.28.0
torch==2.1.2
torchaudio==2.1.2
safetensors==0.4.2
#tokenizers==0.22.0
tokenizers==0.21.4

# --- Intent Router (Sklearn pickle exact version) ---
joblib==1.4.2
scikit-learn==1.6.1     # ★ FIXED: must match your pickled model version

# --- Phase 2A Faster-Whisper (CT2 backend STT) ---
faster-whisper==1.0.3   # ★ REQUIRED: removes ModuleNotFoundError
ctranslate2==4.4.0      # ★ REQUIRED: GPU/CPU safe, works on Render CPU
